import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { uploadApi } from '../services/api';
const types=['ransomware','keylogger','backdoor','worm','trojan','adware'];
export default function Sandbox(){const [type,setType]=useState('ransomware');const [name,setName]=useState('sandbox-sample.txt');const [content,setContent]=useState('');const [busy,setBusy]=useState(false);const nav=useNavigate();const create=async()=>{setBusy(true);try{const r=await uploadApi.createSandboxSample({type,name,content});const scan=await uploadApi.scanSandboxSample(r.data.name);nav(`/live?scanId=${scan.data.scanId}`)}catch(e){alert(e.error||e.message||'Не удалось создать sample')}finally{setBusy(false)}};return <div className="page"><div className="page-head"><div><div className="eyebrow">LOCAL SANDBOX</div><h1>Конструктор тестовых образцов</h1><p>Создаёт только инертные текстовые/JavaScript-образцы с сигнатурами для проверки детектора. Никакого запуска кода.</p></div></div><div className="panel sandbox"><label>Сценарий<select value={type} onChange={e=>setType(e.target.value)}>{types.map(t=><option key={t}>{t}</option>)}</select></label><label>Имя файла<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Содержимое<textarea rows="12" value={content} onChange={e=>setContent(e.target.value)} placeholder="Можно оставить пустым — будет создан безопасный тестовый шаблон."/></label><button className="btn primary" disabled={busy} onClick={create}>{busy?'Создаю и сканирую…':'Создать и сразу сканировать'}</button></div><div className="panel note"><b>Важно:</b> папка <code>backend/sandbox_samples</code> предназначена только для безопасных samples. Загруженные реальные подозрительные файлы анализируются статически и не исполняются.</div></div>}
