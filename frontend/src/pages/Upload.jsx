import {useNavigate} from 'react-router-dom';import FileUpload from '../components/forms/FileUpload';export default function Upload(){const nav=useNavigate();return <div className="page"><div className="page-head"><div><div className="eyebrow">STATIC ANALYSIS</div><h1>Проверка файла</h1><p>Загрузите подозрительный файл. Система читает его как Buffer, строит SHA-256, считает энтропию и ищет сигнатуры. Код не запускается.</p></div></div><FileUpload onUploadComplete={r=>r?.scanId&&nav(`/live?scanId=${r.scanId}`)}/><div className="info-strip"><span><b>01</b> Hash + entropy</span><span><b>02</b> IOC / URLs / IP</span><span><b>03</b> Behavioral emulation</span><span><b>04</b> Verdict + history</span></div></div>}
