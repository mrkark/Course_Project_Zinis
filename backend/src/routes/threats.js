const express=require('express');const Threat=require('../models/Threat');const router=express.Router();router.get('/',async(req,res,next)=>{try{res.json({success:true,data:await Threat.findAll()})}catch(e){next(e)}});router.get('/:type',async(req,res,next)=>{try{const t=await Threat.findByType(req.params.type);if(!t)return res.status(404).json({success:false,error:'Threat not found'});res.json({success:true,data:t})}catch(e){next(e)}});module.exports=router;
